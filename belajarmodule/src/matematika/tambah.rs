use super::bagi::{bagi, tampilkan_pesan};
pub fn tampilkan_hasil() {
    // Contoh: panggil fungsi pesan
    let pesan = tampilkan_pesan("Operasi berhasil!");
    println!("{}", pesan);
}
pub fn fntambah(a: i32, b: i32) -> i32 {
    a + b
}
